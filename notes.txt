(done) Move the show bookmark logic to new page.
(done) Option to add users photo and name and share the quote as an image.
(done) Update the UI to be more attractive specially the buttons.
(done) Remove the index header.
Ability to login.
save the bookmarks to DB instead of asyncStorage if logged in.
Add ads at the bottom to get revenue.
back up 2 more APIs in case one fails.
search and integrate image quotes as well.
different language quotes.
Daily quote reminder.
check if sharechat or similar has API and integrate to get the quotes from there.


********
npx expo prebuild (to generate the native android and ios code. not eject, still can use expo related stuff but can be used to build the apps locally.)
npx expo run:android --variant debug (connect the mobile to laptop and run this cmd to build the app, generate the apk and install to the mobile with hot reload, connceted to dev server.)
npx expo run:android --variant release (same as above but does not connect to metro and hence no need to run the server and no hot reloading.)
expo start -d (if we just want to run the app on mobile. if not installed any new packages then this cmd is enough no need for above cmd.) 
npx expo start -c

************************

For deploying app to play store:
https://www.youtube.com/watch?v=WOKrelHPefc

https://www.youtube.com/watch?v=qxO_8GFAliE - internal testing and next parts too in detail.

*********************************

quotes app improvements

change app name
modify the same app name in the logo text as well.
store around 500 - 1k quotes in some DB or somewhere and get from there when the API fails to get the quotes.


in home screen page: (done)
logo is not required, just nice text is fine.
Improve Button arrangement / layout
theme selection popup is not good looking, improve it to look modern/attractive.
Make theme persisted so when we open the app next time it should be the same theme.

in quotes page(done)
1.give space between heading quotes and the body
2.Buttons should be previous and next (in one row), bookmark, share, "share this quote with customization" in another row below it.
3.change next button icon from refresh to next.
4.change the alert style to a modern one when bookmark is clicked.
5.change the "bookmark" icon to "bookmarked" icon once it is bookmarked, so that user doesnt get confused.
6.If they click "bookmarked" icon it should be removed from the bookmarked list and icon should change back to "bookmark"
7.Button click of "share this quote with customization" which should go to "share your quote" page with this quote filled in.
8.Once the quotes page is loaded, Store the quotes and make/sync the next, previous buttons to next or previous quotes from this collection saved in cache. where next will do a API call, get quote, show in UI, save in that local cache list and new back button will get from the cache list.

in bookmarked quotes page(done)
store bookmarked quotes in asyncstorage.
change the alert style to a modern one when delete bookmark is clicked.
add share button as well on the popup along with delete and close when one bookmarked quote is selected. use the same share option from quotes page to show the popup asking share just text or customize and share.


in share customized quote page(done)
when add random background button is clicked, show 30 secs / 1 min timer with cancel option when the image is being fetched.
and if did not get within that time, stop the processing.


In future
(done)show a disclaimer saying quotes found from online and this app does not own or suggest or represent or anything related to the content showed.
language specific quotes categories (can select multiple languages)
daily quote reminder notification

https://www.kaggle.com/datasets/marufchowdhury/quote-dataset
https://www.kaggle.com/datasets/kimlongphmhong/full-tags-of-quotes-from-goodread


***************
give me proper readme file which will serve as the attractive point to installlers/users with all the features. no need for dev releated stuff.

features:
simple.
no signin/signup process, direct to content.
clean and neat UI (will attach image)
with 10 color themes which applies to whole application.(will attach image)
ability to bookmark quotes.
View bookmarked quotes.(will attach image)
ability to share the quotes as text and also customize it by adding photo, random, picker background image that resizes based on quote size, add custom greeting or text. then share as image.